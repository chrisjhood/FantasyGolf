<h1>Listing picks</h1>

<table>
  <tr>
    <th>Earnings</th>
    <th>Golfer</th>
    <th>Tournament</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @picks.each do |pick| %>
  <tr>
    <td><%= pick.earnings %></td>
    <td><%= pick.golfer_id %></td>
    <td><%= pick.tournament_id %></td>
    <td><%= pick.user_id %></td>
    <td><%= link_to 'Show', pick %></td>
    <td><%= link_to 'Edit', edit_pick_path(pick) %></td>
    <td><%= link_to 'Destroy', pick, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pick', new_pick_path %>
